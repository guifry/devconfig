# Prefix: Ctrl+] (no conflict w/ vim, shell, Claude Code, macOS)
unbind C-b
set -g prefix C-]
bind C-] send-prefix

setw -g mode-keys vi
set -g mouse on

# Pane navigation (hjkl)
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
